// Storybook analyzes files like `manager.ts` statically
// It means we cannot use inside aliases like `@dsfrc/docs`, so to make it convenient for the rest
// to benefit too from some code, we define here the common between the static world and the runtime one
export const mainTargetPort = 6006;
