import { Meta } from '@storybook/blocks';

<Meta title="dsfr-connect/Utilisation/mastodon-v4" />

# mastodon-v4

Work in progress...

Define how to set up (since Ruby, need to copy/paste file?)


WIP

Copy files
And run...

RAILS_ENV=production rails app:update:bin
RAILS_ENV=production OTP_SECRET=precompile_placeholder SECRET_KEY_BASE=precompile_placeholder bundle exec rails assets:clobber
RAILS_ENV=production OTP_SECRET=precompile_placeholder SECRET_KEY_BASE=precompile_placeholder bundle exec rails assets:precompile



RAILS_ENV=development OTP_SECRET=precompile_placeholder SECRET_KEY_BASE=precompile_placeholder bundle exec rails assets:precompile


Use the Mastodon (Dark) if custom CSS applied...

---

TODO:

Ensuite définir sur la présentation dsfr-connect dans le tableau la différence entre les thèmes pour Framework UI, et pour les outils

Add custom CSS...
http://localhost:3000/admin/settings/appearance


It's for both user and admin!!! The other is just a user setting....
